export class Retailer {
  constructor(private id: number,
              private name: string,
              private logoUrl: string,
              private loginUrl: string,
              private forgotPasswordUrl: string,
              private trolleyUrl: string) {
  }
}
